export interface FormatNumberParams {
    input: number | bigint;
    compact?: boolean;
    maxFractionDigits?: number;
    maxSignificantDigits?: number;
    forceSign?: boolean;
    unit?: "%" | "USD" | string;
}
export declare function formatNumber({ input, compact, maxFractionDigits, maxSignificantDigits, forceSign, unit, }: FormatNumberParams): string;

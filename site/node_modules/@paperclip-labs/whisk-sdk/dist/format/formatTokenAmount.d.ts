import { BigIntString } from "../types";
import { FormatNumberParams } from "./formatNumber";
interface FormatTokenAmountParams extends Omit<FormatNumberParams, "input"> {
    tokenAmount: bigint | BigIntString;
    tokenDecimals: number;
}
export declare function formatTokenAmount({ tokenAmount, tokenDecimals, ...rest }: FormatTokenAmountParams): string;
export {};

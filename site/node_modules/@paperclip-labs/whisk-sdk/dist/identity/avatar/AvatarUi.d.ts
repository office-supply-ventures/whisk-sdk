import { HTMLAttributes } from "react";
import { Address } from "viem";
interface AvatarUiProps extends HTMLAttributes<HTMLDivElement> {
    address: Address;
    avatar?: string | null;
    size: number;
}
export declare function AvatarUi({ address, size, avatar, className, ...props }: AvatarUiProps): import("react/jsx-runtime").JSX.Element;
export {};

"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import { formatAddress } from "../../format";
import { cn } from "../../ui";
import { motion, AnimatePresence } from "motion/react";
export function NameUi({ address, name, className, ...props }) {
    return (_jsx("div", { className: cn("relative", className), ...props, children: _jsx(AnimatePresence, { mode: "popLayout", initial: false, children: _jsx(motion.div, { initial: { opacity: 0 }, animate: { opacity: 1 }, exit: { opacity: 0 }, transition: { type: "spring", duration: 0.3, bounce: 0 }, className: cn("text-ellipsis overflow-hidden whitespace-nowrap"), children: name ?? formatAddress({ address }) }, name ? "loaded" : "loading") }) }));
}
